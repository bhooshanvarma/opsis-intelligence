import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Opsis Intelligence - Supply Chain Threat Intelligence',
  description: 'The world\'s first comprehensive supply chain threat intelligence platform',
  keywords: 'supply chain, threat intelligence, risk management, procurement',
  authors: [{ name: 'Opsis Intelligence' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
